name: lokalex_rider_toolkit
description: Lokalex Rider App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  sqflite: ^2.3.0
  path: ^1.8.3
  crypto: ^3.0.7
  intl: ^0.20.3
  shared_preferences: ^2.5.5
  package_info_plus: ^8.1.0
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true